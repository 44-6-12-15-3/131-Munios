#include <iostream>
using namespace std;

void up(int a, int b) {
    if (a > b) return;
    cout << a << " ";
    up(a + 1, b);
}

void down(int a, int b) {
    if (a < b) return;
    cout << a << " ";
    down(a - 1, b);
}

int main() {
    int a, b;
    cin >> a >> b;
    
    if (a < b) up(a, b);
    else down(a, b);
    
    return 0;
}
